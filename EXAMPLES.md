# 📝 Приклади Конфігурацій

Тут зібрані різні варіанти конфігурацій для різних потреб.

---

## 1️⃣ Мінімальна конфігурація (рекомендовано для першого створення)

```python
shape_config = oci.core.models.LaunchInstanceShapeConfigDetails(
    ocpus=1.0,
    memory_in_gbs=6.0
)
```

**Характеристики:**
- 1 CPU (ARM Cortex-A72)
- 6 GB RAM
- Найлегше створити (більше capacity)
- Підходить для: VPN, легкі веб-сервери, development

**Always Free:** ✅ Так

---

## 2️⃣ Збалансована конфігурація

```python
shape_config = oci.core.models.LaunchInstanceShapeConfigDetails(
    ocpus=2.0,
    memory_in_gbs=12.0
)
```

**Характеристики:**
- 2 CPU
- 12 GB RAM
- Гарний баланс продуктивності
- Підходить для: Docker, середні веб-сервіси, бази даних

**Always Free:** ✅ Так

---

## 3️⃣ Максимальна Always Free конфігурація

```python
shape_config = oci.core.models.LaunchInstanceShapeConfigDetails(
    ocpus=4.0,
    memory_in_gbs=24.0
)
```

**Характеристики:**
- 4 CPU
- 24 GB RAM
- Максимум для Always Free
- Підходить для: Kubernetes, важкі додатки, множинні контейнери

**Always Free:** ✅ Так (це ліміт)

**Порада:** Складніше створити через capacity. Краще спочатку створити 1 CPU, потім збільшити.

---

## 4️⃣ Конфігурація для веб-сервера + БД

```python
shape_config = oci.core.models.LaunchInstanceShapeConfigDetails(
    ocpus=2.0,
    memory_in_gbs=16.0
)
```

**Характеристики:**
- 2 CPU
- 16 GB RAM (8 GB на CPU)
- Більше RAM для бази даних
- Підходить для: LAMP/LEMP стек, WordPress з БД

**Always Free:** ✅ Так

---

## 5️⃣ Множинні інстанси (стратегія розподілу)

Замість одного великого, створіть декілька менших:

### Варіант A: 4 мікро-інстанси
```python
# Створіть 4 окремі інстанси по:
ocpus=1.0
memory_in_gbs=6.0

# Розподіл:
# - Instance 1: Web frontend
# - Instance 2: API backend  
# - Instance 3: Database
# - Instance 4: Cache/Queue
```

**Плюси:**
- Легше створити (більше шансів на capacity)
- Ізоляція сервісів
- Можна розподілити по різних регіонах

**Мінуси:**
- Складніше управління
- Більше конфігурації мережі

### Варіант B: 2 середні інстанси
```python
# Instance 1:
ocpus=2.0
memory_in_gbs=12.0

# Instance 2:
ocpus=2.0
memory_in_gbs=12.0

# Розподіл:
# - Instance 1: Production
# - Instance 2: Development/Staging
```

---

## 6️⃣ Конфігурація для Docker/Kubernetes

```python
shape_config = oci.core.models.LaunchInstanceShapeConfigDetails(
    ocpus=3.0,
    memory_in_gbs=18.0
)
```

**Характеристики:**
- 3 CPU
- 18 GB RAM
- Достатньо для k3s або microk8s
- Підходить для: Контейнеризовані додатки, CI/CD

**Always Free:** ✅ Так

**Приклад використання:**
- 1 CPU для ОС
- 2 CPU для контейнерів
- 6 GB для ОС, 12 GB для контейнерів

---

## 🔧 Як змінити конфігурацію в workflow

Відредагуйте файл `.github/workflows/oci-instance-creator.yml`:

```python
# Знайдіть цей блок:
shape_config = oci.core.models.LaunchInstanceShapeConfigDetails(
    ocpus=1.0,        # <--- Змініть тут
    memory_in_gbs=6.0  # <--- І тут
)
```

**Обмеження:**
- Мінімум: 1 OCPU, 6 GB RAM (1 GB на 1 OCPU недостатньо!)
- Максимум Always Free: 4 OCPU, 24 GB RAM
- Крок RAM: 1 GB
- Крок CPU: 0.5 (можна 1.0, 1.5, 2.0, 2.5, 3.0, 3.5, 4.0)

**Приклади валідних конфігурацій:**
```python
# ✅ OK:
ocpus=1.0, memory_in_gbs=6.0
ocpus=1.5, memory_in_gbs=9.0
ocpus=2.0, memory_in_gbs=12.0
ocpus=2.5, memory_in_gbs=15.0
ocpus=3.0, memory_in_gbs=18.0
ocpus=4.0, memory_in_gbs=24.0

# ❌ НЕ OK:
ocpus=1.0, memory_in_gbs=4.0   # Занадто мало RAM
ocpus=5.0, memory_in_gbs=30.0  # Перевищує Always Free
ocpus=1.3, memory_in_gbs=8.0   # Неправильний крок CPU
```

---

## 📊 Порівняльна таблиця

| Конфігурація | CPU | RAM | Складність | Підходить для |
|--------------|-----|-----|------------|---------------|
| Мінімальна | 1 | 6 GB | ⭐ Легко | VPN, DNS, легкі сервіси |
| Збалансована | 2 | 12 GB | ⭐⭐ Середнє | Web + DB, Docker |
| Потужна | 3 | 18 GB | ⭐⭐⭐ Складно | K8s, важкі додатки |
| Максимальна | 4 | 24 GB | ⭐⭐⭐⭐ Дуже складно | Все що завгодно |

**Складність = Наскільки важко знайти capacity**

---

## 🎯 Рекомендації вибору

### Для початківців:
```python
ocpus=1.0
memory_in_gbs=6.0
```
**Чому:** Легко створити, достатньо для більшості задач, можна збільшити пізніше.

### Для веб-розробників:
```python
ocpus=2.0
memory_in_gbs=12.0
```
**Чому:** Оптимально для LAMP/MEAN/MERN стеку.

### Для DevOps:
```python
ocpus=3.0
memory_in_gbs=18.0
```
**Чому:** Достатньо для CI/CD, Docker Compose, k3s.

### Максималісти (не рекомендується для першого разу):
```python
ocpus=4.0
memory_in_gbs=24.0
```
**Чому:** Максимум Always Free, але дуже важко знайти capacity. Краще створити 1 CPU, потім збільшити.

---

## 🔄 Як збільшити конфігурацію після створення

### Варіант 1: Через OCI Console (найпростіше)
1. OCI Console → Compute → Instances
2. Виберіть інстанс → Stop
3. Edit → Change Shape
4. Виберіть нову конфігурацію
5. Start

### Варіант 2: Через OCI CLI
```bash
# Зупинити інстанс
oci compute instance action --action STOP --instance-id <instance-ocid>

# Змінити shape
oci compute instance update \
  --instance-id <instance-ocid> \
  --shape-config '{"ocpus":2.0,"memoryInGBs":12.0}'

# Запустити
oci compute instance action --action START --instance-id <instance-ocid>
```

### Варіант 3: Створити новий та мігрувати
1. Створіть образ з існуючого інстансу
2. Створіть новий інстанс з цього образу з новою конфігурацією
3. Перенесіть публічну IP (якщо потрібно)

---

## 💡 Хитрощі

### Створення складніше → Використовуйте обхідний шлях:

1. **Спочатку створіть мінімальну:**
   ```python
   ocpus=1.0, memory_in_gbs=6.0
   ```

2. **Після створення збільште:**
   ```python
   ocpus=4.0, memory_in_gbs=24.0
   ```

**Чому це працює:**
- Мінімальну легше створити (більше capacity)
- Збільшення існуючого інстансу не потребує нового capacity
- Економить час очікування

### Створюйте в менш популярних AD:

```python
# Замість популярного AD-1:
availability_domain = "iAcH:EU-FRANKFURT-1-AD-1"

# Спробуйте:
availability_domain = "iAcH:EU-FRANKFURT-1-AD-2"  # Або AD-3
```

### Створюйте декілька workflow для різних AD:

Створіть 3 workflow файли:
- `oci-creator-ad1.yml` → AD-1
- `oci-creator-ad2.yml` → AD-2  
- `oci-creator-ad3.yml` → AD-3

Хтось з них швидше знайде capacity!

---

## 📝 Template для копіювання

```python
# Вставте в .github/workflows/oci-instance-creator.yml

# ======== ВИБЕРІТЬ КОНФІГУРАЦІЮ ========

# Мінімальна (рекомендовано):
shape_config = oci.core.models.LaunchInstanceShapeConfigDetails(
    ocpus=1.0,
    memory_in_gbs=6.0
)

# Збалансована:
# shape_config = oci.core.models.LaunchInstanceShapeConfigDetails(
#     ocpus=2.0,
#     memory_in_gbs=12.0
# )

# Потужна:
# shape_config = oci.core.models.LaunchInstanceShapeConfigDetails(
#     ocpus=3.0,
#     memory_in_gbs=18.0
# )

# Максимальна:
# shape_config = oci.core.models.LaunchInstanceShapeConfigDetails(
#     ocpus=4.0,
#     memory_in_gbs=24.0
# )

# ======================================
```

---

Виберіть конфігурацію яка підходить для ваших потреб! 🚀
