# 📁 Структура Проекту

```
oci-a1-auto-creator/
│
├── .github/
│   └── workflows/
│       └── oci-instance-creator.yml    # Головний GitHub Actions workflow
│
├── .gitignore                           # Виключає чутливі файли
│
├── README.md                            # Основна документація
├── QUICKSTART.md                        # Швидкий старт (5 хвилин)
├── SECRETS_TEMPLATE.md                  # Шаблон для GitHub Secrets
├── TROUBLESHOOTING.md                   # Помилки та їх вирішення
│
├── get-oci-info.sh                      # Helper: збір OCID з OCI
└── test-config.py                       # Тестування конфігурації локально
```

---

## 📄 Опис файлів

### Основні файли:

**`.github/workflows/oci-instance-creator.yml`**
- GitHub Actions workflow
- Автоматично запускається кожні 10 хвилин
- Намагається створити A1.Flex інстанс
- Використовує OCI Python SDK

**`README.md`**
- Повна документація проекту
- Інструкції з налаштування
- Пояснення як знайти всі необхідні OCID
- Приклади використання

**`QUICKSTART.md`**
- Швидкий старт за ~20 хвилин
- Покрокова інструкція
- Чеклист налаштування
- Мінімум деталей, максимум дії

**`SECRETS_TEMPLATE.md`**
- Шаблон всіх необхідних GitHub Secrets
- Детальні інструкції де знайти кожен OCID
- Формати та приклади значень
- Важливі примітки

**`TROUBLESHOOTING.md`**
- Поширені помилки та рішення
- Поради для успішного створення
- Статистика та оптимізація
- Діагностика проблем

---

### Helper скрипти:

**`get-oci-info.sh`**
- Автоматично збирає всі OCID з OCI
- Показує доступні Availability Domains
- Знаходить ARM образи для A1
- Відображає VCN та Subnets
- **Потребує:** OCI CLI встановлено та налаштовано

```bash
# Використання:
chmod +x get-oci-info.sh
./get-oci-info.sh
```

**`test-config.py`**
- Тестує OCI конфігурацію локально
- Перевіряє всі OCID перед GitHub
- Валідує API credentials
- Показує чи Image підходить для A1
- **Потребує:** OCI CLI налаштовано, Python 3, `pip install oci`

```bash
# Використання:
python3 test-config.py
```

**`.gitignore`**
- Виключає чутливі файли (.pem, .oci/)
- Виключає Python cache
- Захист від випадкового коміту credentials

---

## 🚀 Швидке розгортання

### Варіант 1: Прямо на GitHub

1. Створіть новий репозиторій на GitHub
2. Завантажте тільки `.github/workflows/oci-instance-creator.yml`
3. Додайте всі Secrets (див. SECRETS_TEMPLATE.md)
4. Workflow запуститься автоматично

### Варіант 2: Клонувати та налаштувати

```bash
# 1. Створіть папку
mkdir oci-a1-creator
cd oci-a1-creator

# 2. Скопіюйте файли з архіву
# (розпакуйте завантажений архів)

# 3. Ініціалізуйте git
git init
git add .
git commit -m "Initial commit"

# 4. Створіть repo на GitHub та залінкуйте
git remote add origin https://github.com/your-username/oci-a1-creator.git
git push -u origin main

# 5. Додайте Secrets в GitHub Settings
```

### Варіант 3: З локальним тестуванням

```bash
# 1. Встановіть OCI CLI
pip install oci-cli
oci setup config

# 2. Запустіть helper для збору OCID
chmod +x get-oci-info.sh
./get-oci-info.sh

# 3. Протестуйте конфігурацію
python3 test-config.py

# 4. Коли все OK - розгортайте на GitHub
```

---

## 📋 Чеклист використання

### Перед запуском:
- [ ] Прочитано QUICKSTART.md
- [ ] API ключ створено в OCI
- [ ] Всі OCID зібрано (вручну або через get-oci-info.sh)
- [ ] Конфігурація протестована локально (опціонально)
- [ ] Репозиторій створено на GitHub
- [ ] 11 Secrets додано в GitHub
- [ ] Workflow файл завантажено
- [ ] Перший запуск виконано

### Під час роботи:
- [ ] Перевіряю логи кожні 6-12 годин
- [ ] Workflow активний та запускається
- [ ] Немає помилок авторизації
- [ ] Бачу "Out of capacity" - все працює правильно

### Після успіху:
- [ ] Workflow вимкнено/видалено
- [ ] Інстанс запущено та доступний
- [ ] SSH підключення працює
- [ ] Firewall налаштовано
- [ ] Необхідне ПЗ встановлено
- [ ] API ключ видалено (якщо не потрібен)

---

## 💾 Що зберігати локально

**Обов'язково зберегти:**
- 🔑 Private API Key (.pem файл) - в безпечному місці
- 🔑 SSH приватний ключ (~/.ssh/id_rsa) - для підключення до інстансу

**Опціонально зберегти:**
- 📋 Копію всіх OCID у текстовому файлі
- 📋 Налаштування мережі (VCN, Subnet IDs)
- 📋 Скріншоти успішного створення

**НЕ зберігати в git:**
- ❌ .pem файли
- ❌ Приватні SSH ключі  
- ❌ OCI config файли
- ❌ Будь-які credentials

---

## 🔄 Оновлення

Якщо OCI змінить API або з'являться нові features:

1. Оновіть workflow файл з нової версії
2. Перевірте чи secrets змінились
3. Протестуйте локально через test-config.py
4. Запушіть зміни

---

## 📊 Розміри файлів

```
oci-instance-creator.yml  ~4 KB    # Workflow
README.md                 ~7 KB    # Документація
QUICKSTART.md             ~8 KB    # Швидкий старт
SECRETS_TEMPLATE.md       ~5 KB    # Шаблон
TROUBLESHOOTING.md        ~10 KB   # Помилки
get-oci-info.sh           ~3 KB    # Helper bash
test-config.py            ~8 KB    # Helper Python
.gitignore                ~0.5 KB  # Gitignore

Всього:                   ~45 KB
```

---

## 🎯 Мінімальний набір для запуску

Якщо потрібно ТІЛЬКИ базова функціональність без helper'ів:

**Необхідно:**
- `.github/workflows/oci-instance-creator.yml`
- Secrets в GitHub (11 штук)

**Опціонально:**
- README.md (для інструкцій)
- SECRETS_TEMPLATE.md (для довідки)

Все інше - додаткові helper'и та документація для зручності.

---

## 📞 Підтримка структури

Якщо додаєте власні features:

```
oci-a1-auto-creator/
│
├── .github/workflows/
│   ├── oci-instance-creator.yml      # Основний
│   ├── notify-telegram.yml           # Ваш: сповіщення
│   └── cleanup-old-instances.yml     # Ваш: очистка
│
├── scripts/
│   ├── get-oci-info.sh
│   ├── test-config.py
│   └── your-custom-script.sh         # Ваші helper'и
│
├── docs/
│   ├── README.md
│   ├── QUICKSTART.md
│   ├── SECRETS_TEMPLATE.md
│   ├── TROUBLESHOOTING.md
│   └── ADVANCED.md                   # Ваша документація
│
└── examples/
    ├── 2cpu-12gb.yml                 # Приклад іншої конфігурації
    └── multiple-regions.yml          # Приклад multi-region
```

---

Успіхів! 🚀
