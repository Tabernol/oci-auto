# 🚀 Швидкий Старт

## Кроки для запуску автоматичного створення A1 інстансу:

### 1️⃣ Підготовка (5 хвилин)

#### Створіть API ключ в OCI:
```bash
# В OCI Console:
Profile (праворуч вгорі) → User Settings → API Keys → Add API Key
→ Generate API Key Pair → Download Private Key → Add
→ Збережіть .pem файл та скопіюйте Fingerprint
```

#### Згенеруйте SSH ключ (якщо немає):
```bash
ssh-keygen -t rsa -b 4096
cat ~/.ssh/id_rsa.pub  # Скопіюйте це
```

---

### 2️⃣ Збір інформації (10 хвилин)

#### Варіант А: Автоматично (якщо є OCI CLI):
```bash
# Встановіть OCI CLI
brew install oci-cli
oci setup config  # Налаштуйте з вашим API ключем

# Запустіть helper
chmod +x get-oci-info.sh
./get-oci-info.sh

# Протестуйте конфігурацію
python3 test-config.py
```

#### Варіант Б: Вручну через консоль:
Відкрийте `SECRETS_TEMPLATE.md` - там детальні інструкції де знайти кожен OCID.

**Мінімально потрібні дані:**
- User OCID → Profile → User Settings
- Tenancy OCID → Profile → Tenancy  
- Fingerprint → з попереднього кроку
- Private Key → вміст .pem файлу
- Region → напр. `eu-frankfurt-1`
- Compartment OCID → Identity & Security → Compartments
- Availability Domain → напр. `iAcH:EU-FRANKFURT-1-AD-1`
- Subnet OCID → Networking → VCN → Subnets
- Image OCID → образ для ARM (Ubuntu 22.04 ARM або Oracle Linux 8 ARM)
- SSH Public Key → з `~/.ssh/id_rsa.pub`

---

### 3️⃣ GitHub налаштування (5 хвилин)

#### Створіть репозиторій:
```bash
# Локально або на GitHub
mkdir oci-a1-creator
cd oci-a1-creator

# Створіть структуру
mkdir -p .github/workflows
# Скопіюйте файл oci-instance-creator.yml в .github/workflows/
```

#### Додайте Secrets:
```
GitHub → Settings → Secrets and variables → Actions → New repository secret

Додайте по черзі:
✅ OCI_USER_OCID
✅ OCI_TENANCY_OCID  
✅ OCI_FINGERPRINT
✅ OCI_PRIVATE_KEY (весь вміст .pem включно з BEGIN/END)
✅ OCI_REGION
✅ OCI_COMPARTMENT_OCID
✅ OCI_AVAILABILITY_DOMAIN
✅ OCI_SUBNET_OCID
✅ OCI_IMAGE_OCID
✅ SSH_PUBLIC_KEY
✅ INSTANCE_NAME
```

---

### 4️⃣ Запуск (1 хвилина)

```bash
# Закомітьте та запушіть
git add .github/workflows/oci-instance-creator.yml
git commit -m "Add OCI A1 auto-creator workflow"
git push

# Workflow запуститься автоматично кожні 10 хвилин!
```

#### Або запустіть вручну:
```
GitHub → Actions → "OCI A1 Instance Creator" → Run workflow
```

---

### 5️⃣ Моніторинг

#### Перегляд логів:
```
GitHub → Actions → виберіть останній запуск → "Try to create A1 instance"
```

#### Що побачите:
```
❌ Немає capacity - продовжує пробувати
✅ УСПІХ! Instance ID: ocid1.instance... - інстанс створено!
```

---

### 6️⃣ Після успішного створення

#### ⚠️ ВАЖЛИВО - Вимкніть workflow:
```bash
# Видаліть файл
rm .github/workflows/oci-instance-creator.yml
git commit -am "Disable workflow after success"
git push

# Або вимкніть в GitHub:
Actions → workflow → ... → Disable workflow
```

#### Підключіться до інстансу:
```bash
# Дізнайтесь публічну IP в OCI Console
ssh ubuntu@<public-ip>  # для Ubuntu образів
# або
ssh opc@<public-ip>     # для Oracle Linux образів
```

---

## 📊 Очікування

**Середній час створення:** 6-48 годин (залежить від capacity в регіоні)

**Найкращі регіони для Always Free A1:**
- `eu-frankfurt-1` - зазвичай швидше
- `eu-amsterdam-1` - альтернатива
- `us-ashburn-1` - США East
- `uk-london-1` - Великобританія

**Порада:** Якщо в одному регіоні довго не виходить, спробуйте змінити:
- Регіон
- Availability Domain (AD-1, AD-2, AD-3)
- Час доби (вночі частіше вдається)

---

## ⚠️ Troubleshooting

### Workflow не запускається:
- Перевірте що файл в правильній папці: `.github/workflows/`
- Перевірте YAML синтаксис (відступи важливі!)

### Помилка авторизації:
- Перевірте всі OCID secrets
- Private Key має містити `-----BEGIN/END-----`
- Fingerprint відповідає Private Key

### "Out of capacity" постійно:
- Це нормально, треба чекати
- Змініть Availability Domain
- Спробуйте інший регіон
- Пробуйте вночі (UTC 00:00-06:00)

### Image не підходить для A1:
- Використовуйте ARM (aarch64) образи
- Oracle Linux 8 ARM
- Ubuntu 22.04/20.04 ARM (не AMD64!)

---

## 💡 Додаткові поради

### Збільшити конфігурацію після створення:
```python
# В workflow файлі змініть:
ocpus=2.0,          # 1-4 CPU
memory_in_gbs=12.0  # 6-24 GB RAM
```

### Змінити частоту спроб:
```yaml
schedule:
  - cron: '*/5 * * * *'   # Кожні 5 хвилин (швидше)
  - cron: '*/15 * * * *'  # Кожні 15 хвилин (економніше)
  - cron: '0 * * * *'      # Кожну годину
```

### Створити декілька інстансів:
Після першого успіху, змініть INSTANCE_NAME та повторіть процес.
Always Free дозволяє до 4 OCPU / 24 GB RAM сумарно.

---

## 📞 Підтримка

**Логи показують конкретну помилку?** Перевірте:
- Всі OCID правильні
- Region співпадає з Image OCID
- Subnet в тому ж регіоні
- API ключ активний

**Все ще не працює?** 
- Запустіть `test-config.py` локально
- Перевірте чи є права на створення інстансів
- Перевірте квоти в OCI (Service Limits)

---

## ✅ Чеклист

Перед запуском переконайтесь:

- [ ] API ключ створено та збережено
- [ ] SSH ключ є (приватний на вашому комп'ютері, публічний в secrets)
- [ ] Всі 11 secrets додано в GitHub
- [ ] Workflow файл в `.github/workflows/`
- [ ] Image OCID підходить для ARM архітектури
- [ ] Subnet в тому ж регіоні що й інстанс
- [ ] Репозиторій запушено на GitHub

**Готово? Запускайте і чекайте notification про успіх! 🎉**

---

**Час створення цієї інструкції: ~20 хвилин**  
**Час роботи після запуску: автоматично 24/7**  
**Успішність: 95%+ (якщо правильно налаштовано)**

Успіхів! 🚀
