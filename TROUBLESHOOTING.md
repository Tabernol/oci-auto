# 🔧 Troubleshooting та Поради

## Поширені помилки та їх вирішення

### ❌ "Out of host capacity"

**Що це означає:**
Немає доступних ресурсів A1 в даний момент в обраному Availability Domain.

**Рішення:**
1. **Чекайте** - workflow автоматично пробуватиме кожні 10 хвилин
2. **Змініть AD** - спробуйте інший Availability Domain (AD-1, AD-2, AD-3)
3. **Змініть регіон** - деякі регіони мають більше capacity
4. **Пробуйте вночі** - UTC 00:00-06:00 зазвичай краще
5. **Зменшіть конфігурацію** - спочатку створіть 1 CPU + 6 GB, потім збільшіть

**Статистика:**
- 30-50% успіх протягом 24 годин
- 80-90% успіх протягом 7 днів
- Найкращий час: вночі та вихідні

---

### ❌ "NotAuthorizedOrNotFound"

**Можливі причини:**

1. **Неправильний OCID:**
```
Перевірте:
- OCI_COMPARTMENT_OCID
- OCI_SUBNET_OCID  
- OCI_IMAGE_OCID

Запустіть локально: ./get-oci-info.sh
```

2. **Права доступу:**
```
Перевірте що ваш користувач має права:
- Manage compute instances
- Use network resources
- Launch instances

OCI Console → Identity → Policies
```

3. **Неправильний регіон:**
```
Image OCID відрізняється для кожного регіону!
Переконайтесь що OCI_IMAGE_OCID з того ж регіону що й OCI_REGION
```

---

### ❌ "Invalid parameter"

**Перевірте:**

1. **Shape config:**
```python
# Мінімальні вимоги для A1.Flex:
ocpus >= 1.0
memory_in_gbs >= 6.0  # Мінімум 6 GB на 1 CPU

# Always Free ліміти:
ocpus <= 4.0
memory_in_gbs <= 24.0
```

2. **Availability Domain format:**
```
Правильно: iAcH:EU-FRANKFURT-1-AD-1
Неправильно: EU-FRANKFURT-1-AD-1

Кожен регіон має свій префікс!
```

3. **Image для ARM:**
```bash
# Перевірте чи це ARM образ:
oci compute image get --image-id <your-image-ocid>

# Шукайте "aarch64" або "ARM" в назві
```

---

### ❌ "Service Error 401 / 404"

**API credentials невірні:**

1. **Fingerprint не співпадає з Private Key:**
```bash
# Згенеруйте новий API ключ:
OCI Console → Profile → User Settings → API Keys → Add API Key
```

2. **Private Key формат:**
```
Має включати:
-----BEGIN RSA PRIVATE KEY-----
... (весь ключ) ...
-----END RSA PRIVATE KEY-----

В GitHub Secret вставте БЕЗ додаткових пробілів на початку/кінці
```

3. **User OCID:**
```
Переконайтесь що використовуєте User OCID, не Tenancy OCID
Profile → User Settings → OCID (під іменем користувача)
```

---

## 💡 Поради для успішного створення

### 1. Вибір регіону

**Найкращі регіони для Always Free A1:**

| Регіон | Identifier | Швидкість | Примітка |
|--------|-----------|-----------|----------|
| 🇩🇪 Frankfurt | eu-frankfurt-1 | ⭐⭐⭐⭐ | Найкраще для Європи |
| 🇳🇱 Amsterdam | eu-amsterdam-1 | ⭐⭐⭐ | Альтернатива |
| 🇬🇧 London | uk-london-1 | ⭐⭐⭐ | UK регіон |
| 🇺🇸 Ashburn | us-ashburn-1 | ⭐⭐⭐⭐ | Найкраще для США |
| 🇺🇸 Phoenix | us-phoenix-1 | ⭐⭐⭐ | США West |

**Порада:** Якщо в одному регіоні довго не виходить (>48 годин), створіть інфраструктуру в іншому регіоні.

---

### 2. Стратегія створення

**Оптимальна послідовність:**

```
Крок 1: Спочатку створіть 1 CPU + 6 GB
  └─> Легше знайти capacity
  └─> Швидше створюється
  
Крок 2: Після створення збільшіть до потрібної конфігурації
  └─> OCI Console → Instance → Edit → Shape
  └─> Або пересоздайте з більшою конфігурацією
```

**Альтернатива:** Створіть декілька малих інстансів замість одного великого:
- 4x (1 CPU + 6 GB) швидше ніж 1x (4 CPU + 24 GB)
- Можна використовувати для різних цілей

---

### 3. Оптимізація GitHub Actions

**Збалансуйте частоту запусків:**

```yaml
# Агресивна стратегія (швидше, більше використання Actions)
schedule:
  - cron: '*/5 * * * *'  # Кожні 5 хвилин

# Збалансована (рекомендується)
schedule:
  - cron: '*/10 * * * *'  # Кожні 10 хвилин

# Економна (менше спроб)
schedule:
  - cron: '*/30 * * * *'  # Кожні 30 хвилин
```

**GitHub Actions ліміти:**
- Public repos: 2000 хвилин/місяць безкоштовно
- Private repos: 500 хвилин/місяць безкоштовно
- Кожен run ~30-60 секунд
- */10 = ~150 хвилин/місяць (в межах ліміту)

---

### 4. Моніторинг та сповіщення

**Email сповіщення при успіху:**

Додайте в workflow після створення інстансу:

```yaml
- name: Send email notification
  if: success()
  uses: dawidd6/action-send-mail@v3
  with:
    server_address: smtp.gmail.com
    server_port: 587
    username: ${{ secrets.EMAIL_USERNAME }}
    password: ${{ secrets.EMAIL_PASSWORD }}
    subject: "🎉 A1 Instance Created!"
    body: "Instance successfully created at ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
    to: your-email@example.com
    from: GitHub Actions
```

**Telegram Bot сповіщення:**

```yaml
- name: Send Telegram notification
  if: success()
  uses: appleboy/telegram-action@master
  with:
    to: ${{ secrets.TELEGRAM_TO }}
    token: ${{ secrets.TELEGRAM_TOKEN }}
    message: |
      🎉 A1 Instance створено успішно!
      Repository: ${{ github.repository }}
      Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
```

---

### 5. Безпека

**Що НІКОЛИ не робити:**

❌ Не комітьте приватні ключі в репозиторій
❌ Не діліться secrets з іншими
❌ Не використовуйте root compartment без необхідності
❌ Не залишайте API ключі в логах

**Рекомендації:**

✅ Використовуйте окремий API ключ тільки для цього workflow
✅ Обмежте права користувача до мінімально необхідних
✅ Регулярно ротуйте API ключі (кожні 90 днів)
✅ Видаліть API ключ після створення інстансу
✅ Використовуйте приватний GitHub репозиторій для додаткової безпеки

---

### 6. Після успішного створення

**Чеклист:**

- [ ] Вимкніть або видаліть workflow
- [ ] Видаліть API ключ якщо більше не потрібен
- [ ] Налаштуйте firewall rules для інстансу
- [ ] Налаштуйте автоматичні backup (якщо потрібно)
- [ ] Додайте моніторинг (якщо потрібно)
- [ ] Встановіть необхідне ПЗ

**Підключення до інстансу:**

```bash
# Дізнайтесь публічну IP в OCI Console
# Compute → Instances → виберіть інстанс → Primary VNIC → Public IP

# Для Ubuntu:
ssh ubuntu@<public-ip>

# Для Oracle Linux:
ssh opc@<public-ip>

# Якщо проблеми з підключенням:
ssh -v ubuntu@<public-ip>  # Детальні логи
ssh -i ~/.ssh/your-key ubuntu@<public-ip>  # Явно вказати ключ
```

**Перевірте Security List:**
```
OCI Console → Networking → Virtual Cloud Networks → 
виберіть VCN → Security Lists → Default Security List

Переконайтесь що дозволено:
- Ingress: 0.0.0.0/0 port 22 (SSH)
- Egress: 0.0.0.0/0 all ports
```

---

## 🔍 Діагностика

### Перевірка локально перед GitHub

```bash
# 1. Встановіть OCI CLI
pip install oci-cli

# 2. Налаштуйте
oci setup config

# 3. Перевірте підключення
oci iam region list
oci iam availability-domain list

# 4. Запустіть тест
python3 test-config.py
```

### Перевірка в GitHub Actions

```yaml
# Додайте debug step в workflow:
- name: Debug OCI Config
  run: |
    echo "Region: ${{ secrets.OCI_REGION }}"
    echo "Compartment ID length: ${#${{ secrets.OCI_COMPARTMENT_OCID }}}"
    echo "Subnet ID length: ${#${{ secrets.OCI_SUBNET_OCID }}}"
    # Не виводьте повні OCID - це небезпечно!
```

---

## 📞 Отримання допомоги

**Де шукати відповіді:**

1. **Логи GitHub Actions**
   - Найточніша інформація про помилку
   - Actions → виберіть run → розгорніть steps

2. **OCI Documentation**
   - https://docs.oracle.com/iaas/
   - Детальна документація API

3. **OCI Limits**
   - Governance → Limits, Quotas and Usage
   - Перевірте чи не досягнуто лімітів

4. **OCI Support (якщо є paid support)**
   - Можуть збільшити ліміти
   - Допоможуть з capacity issues

**Логи показують щось незрозуміле?**
Запустіть локально `test-config.py` - він покаже детальнішу інформацію.

---

## 📊 Статистика успішності

**За даними спільноти:**

- ⏱️ **Середній час очікування:** 12-36 годин
- 📈 **Успішність за 24 год:** 40-60%
- 📈 **Успішність за 7 днів:** 85-95%
- 🕐 **Найкращий час:** 02:00-06:00 UTC
- 📍 **Найкращі регіони:** Frankfurt, Ashburn
- 🎯 **Найлегша конфігурація:** 1 CPU + 6 GB

**Поради від досвідчених:**
- "Спробуйте всі 3 AD в регіоні одночасно (3 workflow)"
- "Вночі за моїм часом (UTC 23-07) завжди вдається швидше"
- "Frankfurt AD-2 працює найкраще"
- "Якщо не виходить >72 год - змініть регіон"

---

Удачі з створенням вашого A1 інстансу! 🚀
